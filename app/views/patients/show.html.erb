<div class='page-header'>
  <h1><%= @patient.first_name + ' ' + @patient.last_name %></h1>
</div>

<div class="page-header">
  <h2>Prescriptions</h2>
</div>
<table class="table table-bordered table-striped">
  <thead>
    <tr>
      <th>Name</th>
    </tr>
  </thead>
  <tbody>
    <!-- @patients.each do |patient| -->
      <tr>
        <!-- <td>link_to full_name(patient), patient_path(patient)</td> -->
      </tr>
    <!-- end -->
  </tbody>
</table>
